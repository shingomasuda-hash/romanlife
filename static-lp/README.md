# ロマンライフ 説明会・オープン・カンパニー2026 ／ 静的LP（公開用）

デザインカンプをもとに、HTML / CSS / JavaScript で実装した
2028年卒向け 説明会（オープン・カンパニー）LPです。**これが公開用の本体です。**

**文字入りの画像を貼っただけのページではありません。** 見出し、本文、カード、ボタン、
日程選択、申込フォーム、図解はすべて実際のHTML要素として実装しています。
そのため、文字の拡大、テキスト選択、翻訳、検索、読み上げがそのまま機能し、
スマートフォンでも文字が縮小されません。

画像として使っているのは、**写真とブランドロゴだけ**です。

> カンプ画像をそのまま連結した参考版は `../image-lp/` に残しています。
> 見た目はカンプそのものですが、文字が画像のままなので可読性・運用性で不利です。

---

## 1. 使い方

ビルド不要です。`index.html` をブラウザで開けばそのまま表示されます。

```bash
# ローカルで確認する場合（webフォント等のパス解決のため簡易サーバー推奨）
cd static-lp
python3 -m http.server 3000     # → http://localhost:3000
```

公開時は `static-lp/` の中身をそのままWebサーバーへ配置してください。
外部CDNへの通信は一切ありません（フォントも同梱）。

```
static-lp/
├─ index.html          全セクションのマークアップ
├─ styles.css          デザインシステム＋各セクションのスタイル
├─ script.js           日程データ／フォーム処理／CTA連動
└─ assets/
   ├─ favicon.svg      暫定ファビコン（★公式のものへ差し替えてください）
   ├─ fonts/           明朝・欧文セリフ（使用文字のみにサブセット化）
   └─ images/          カンプから切り出した写真（WebP）
```

---

## 2. デザインシステム

| 用途 | 値 |
| --- | --- |
| 深いグリーン | `#0B4A35`（CTA・見出し・カード） |
| さらに濃いグリーン | `#063526`（深緑セクションの背景） |
| アイボリー | `#F8F3E8`（ページ背景） |
| 白 | `#FFFFFF`（入力欄・吹き出し） |
| ゴールド | `#B8954A`（罫線・番号・装飾） |
| 本文 | `#263B32` |

- 見出し・数字：明朝体（Shippori Mincho をサブセット化して同梱）
- 本文・UI・フォーム：端末標準のゴシック体（Hiragino Sans / Yu Gothic / Noto Sans JP）
- 英字（SECTION 0X / ROMANLIFE）：Cormorant Garamond（同梱）

色は `styles.css` 冒頭の `:root` にまとめています。ここを変えれば全体へ反映されます。

---

## 3. ★ 開催日・時間・締切の変更

**`script.js` の `EVENT_DATES` だけ**を編集してください。
日程カード、申込フォームの選択肢、最終CTAの日程表示、確認画面、完了画面が
すべて同じデータを参照しています。

締切は `2026-08-11T00:00:00+09:00` のように **日本時間の絶対時刻**で持っているため、
閲覧者の端末のタイムゾーン設定に関わらず、同じ瞬間に締め切られます。

締切を過ぎた日程は自動的に次の状態になります。

- 日程カード … 「受付終了」を表示し、選択ボタンを非表示
- 申込フォーム … 選択肢を `disabled` にして「（受付終了）」を表示
- 全日程が締切 … フォーム全体を「受付は終了しました」に切り替え

---

## 4. ★ 送信先（API）の設定

`script.js` の先頭に設定箇所をまとめています。**ダミーの外部URLは設定していません。**

```js
var ENTRY_API = null;          // 例: '/api/event-entry'
var PRIVACY_POLICY_URL = '';   // 例: 'https://www.romanlife.co.jp/privacy/'
```

- `ENTRY_API` が `null` のあいだは送信を行わず、確認画面から完了画面へ進むだけの動作になります。
- URLを設定すると、`buildPayload()` が組み立てたJSONを `POST` します。
- レスポンスは `{ "ok": true }` を成功とみなします。
  `{ "ok": true, "receiptNumber": "OC-260728-XXXXXX" }` を返すと、完了画面に受付番号を表示します。
  失敗時は `{ "ok": false, "message": "…" }` を返してください（メッセージが確認画面に表示されます）。

送信されるデータ（`buildPayload()` 参照）:

```
submissionId / renderedAt / eventDateId / eventDateDisplay /
sessionId / sessionLabel / sessionTime /
lastName / firstName / lastKana / firstKana /
school / faculty / graduation / email / phone / referral / question /
privacyAgreed / privacyAgreedAt / companyName（ハニーポット）/
source{ utm_source, utm_medium, utm_campaign, utm_content, utm_term, referrer, pageUrl }
```

> **サーバー側でも必ず再検証してください。**
> 締切、必須項目、メール・電話の形式、`companyName` が空であること（ボット判定）は、
> ブラウザ側の検証だけに頼らず、受け口側で必ず確認してください。

`PRIVACY_POLICY_URL` が未設定のあいだは、仮URLを置かずに
「未設定です」と画面に明示されます。公開前に必ず設定してください。

---

## 5. 実装している動作

| 機能 | 内容 |
| --- | --- |
| CTA | ページ内のすべてのCTAが最下部の申込フォーム（`#entry-form`）へ移動。外部媒体へは遷移しません |
| 日程カード連動 | ボタンを押すと参加希望日がフォームへ反映され、フォームへスクロール後、参加希望時間へフォーカス |
| 入力 → 確認 → 完了 | 送信は「この内容で申し込む」を押したときのみ。「修正する」で入力内容を保持したまま戻れます |
| 入力エラー | フォーム上部のサマリー（クリックで該当項目へジャンプ）＋各項目の直下に文章とアイコンで表示 |
| 二重送信防止 | 送信中はボタンを無効化し「送信しています…」を表示。多重リクエストを抑止 |
| 締切制御 | 上記3章のとおり |
| 固定CTA | スマートフォンのみ表示。フォーム到達中・入力中・確認/完了画面では自動的に非表示 |
| 計測 | `window.dataLayer` へ push（`cta_click` / `form_confirm_view` / `entry_complete` / `entry_error`）。GTM等を後から追加できます |

---

## 6. 表示確認の結果

375 / 390 / 414 / 430 / 768 / 1280 / 1920px で確認済みです。

- 横スクロール：**発生なし**（全幅で `scrollWidth === clientWidth`）
- コンソールエラー：**なし**
- 外部通信：**なし**（フォント・画像はすべて同梱）
- axe-core（WCAG 2.0/2.1 A・AA）：**違反 0件**（1280 / 390 / 375px すべて）
- 入力文字・本文は 16px 未満にしていません
- ボタン・選択肢のタップ領域は 44px 以上（主要ボタンは 62px）

動作テスト（390px）:

| 項目 | 結果 |
| --- | --- |
| FVのCTA → フォームへ移動 | OK |
| 開催日程カード（FV）の表示 | OK |
| 日程カード → 参加希望日へ反映 | OK |
| 参加希望時間へフォーカス誘導 | OK |
| 未入力時のエラー表示（12項目） | OK |
| 「その他」選択で追加入力欄が出る／戻すと隠れる | OK |
| 同意なしでは確認画面へ進めない | OK |
| 電話番号のハイフン有無を吸収（`090-1234-5678` → `09012345678`） | OK |
| 確認 → 修正 → 入力内容の保持 | OK |
| 送信ボタン連打でも1回だけ送信 | OK |
| 確認画面・完了画面で見出しへフォーカス移動 | OK |
| フォーム表示中は固定CTAが隠れる | OK |

---

## 7. ★ 公開前に確認・差し替えが必要なもの

| 項目 | 現状 | 対応 |
| --- | --- | --- |
| **写真** | ファーストビュー・SECTION 02・SECTION 03「ロマンライフを知る」は**ご支給の実写**。それ以外はデザインカンプから切り出したAI生成画像 | 残りも実写へ差し替えてください（→ 8章） |
| **ブランドロゴ** | ご支給のFV画像から切り出し、周囲の背景のみ透明化したPNG | 公式のロゴデータ（SVG / 透明PNG）へ差し替えてください（→ 9章） |
| **送信先API** | 未設定（`ENTRY_API = null`） | 4章のとおり設定 |
| **プライバシーポリシーURL** | 未設定（画面に注意書きが出ます） | `PRIVACY_POLICY_URL` を設定 |
| **ファビコン** | 暫定の抽象マーク（企業ロゴ不使用） | `assets/favicon.svg` を差し替え |
| **OGP画像** | `assets/images/ogp.png`（自作） | 必要に応じて差し替え |

### 公式情報が確認できず、意図的に記載していないもの

- 参加費、具体的な定員・座席数
- 服装・持ち物（「申込み後の参加案内をご確認ください」としています）
- 当日の受付場所・入口（同上）
- 従業員数・店舗数（出典により数値が異なるため）

### ご支給資料（PDF「LP修正点」「LP修正指示」）から反映した内容

すべて、いただいた赤字・テキストのとおりに反映しています。

掲載している数字は、公開情報で確認できた
**1951年（創業）／70年以上／1982年（マールブランシュ誕生）／3ブランド** のみです。

---

## 8. 写真の差し替え方

`assets/images/` の同名ファイルを置き換えるだけで反映されます（WebP推奨）。

| ファイル | 使用箇所 | 推奨比率 |
| --- | --- | --- |
| `fv-staff.webp` | ファーストビュー（PC・幅768px以上） | 横長（16:7で切り出されます） |
| `fv-staff-sp.webp` | ファーストビュー（スマートフォン） | 4:3（PC版より寄りで切り出したもの） |
| `intro-handover.webp` | SECTION 02 大きい写真 | 9:10 |
| `intro-family.webp` / `intro-sweets.webp` | SECTION 02 小さい写真 | 16:9 前後 |
| `benefit-01〜04.webp` | SECTION 03 各カード | 16:10 |
| `prog-01,03,04,05,06,07.webp` | SECTION 04 タイムライン | 3:2 |
| `company-store.webp` | SECTION 05・アクセス・最終CTA背景 | 縦長（4:5） |
| `company-case.webp` | SECTION 05 小さい写真 | 5:4 |
| `career-staff.webp` | SECTION 06 上部 | 9:10 |
| `career-handover.webp` | SECTION 06 下部 | 4:3 |
| `voice-01〜03.webp` | SECTION 07 社員3名 | 3:4 |

> 現在の写真はデザインカンプから切り出したAI生成画像です。実在の社員ではないため、
> 採用ページとして公開する前に、実際の社員・店舗写真へ差し替えることを強く推奨します。

---

## 9. ファーストビューについて

ファーストビューは、ご支給いただいたFV画像（`../image-lp/assets/panels/p01.webp`）を
そのまま貼るのではなく、**同じ構成・同じ文言のままHTML/CSSへ組み直しています。**

| カンプの要素 | 実装 |
| --- | --- |
| 上部の帯「2028年卒対象／採用説明会・オープン・カンパニー」 | HTML＋CSS（`.hero-ribbon`） |
| 「京都洋菓子ブランド」「を展開する」「株式会社ロマンライフ」 | HTMLテキスト |
| MALEBRANCHE ／ ROMANLIFE のロゴ | **画像**（`assets/images/logo-*.png`） |
| 「2028 GRADUATES」 | HTMLテキスト＋CSS（`.hero-grad`） |
| 見出し・リード文 | HTMLテキスト |
| 「参加者限定 特別選考ルートをご案内」の枠 | HTML＋CSS＋SVG（王冠・月桂樹） |
| 開催日程①② のカード | HTML＋CSS（`.hd-card`） |
| 深緑のCTAバー | HTML＋CSS |
| 写真（社員・お菓子） | **画像**（`assets/images/fv-staff.webp`） |

### ロゴの扱い

`logo-malebranche.png` / `logo-romanlife.png` は、ご支給画像から該当箇所を切り出し、
**周囲の明るい背景のみを透明化**したものです。ロゴの形・色・字間は変更していません。

ただし元が写真上のラスター画像のため、拡大するとエッジが甘くなります。
**公式のロゴデータ（SVG または背景透明のPNG）をいただければ差し替えます。**

### FV写真の切り出し範囲

ご支給画像には見出しの文字が写真に重なって焼き込まれているため、
**文字が重なっていない右側（男性社員の右隣から右端まで）**を切り出しています。
そのため、男性社員はほとんど写っていません。

**文字が入っていない元の写真をいただければ、2人が写った構図に差し替えます。**


---

## 10. ご支給資料（PDF）からの反映一覧

| 箇所 | 変更内容 |
| --- | --- |
| 開催日程 | **9月4日（金）を追加**（全3日程） |
| 申込締切 | 当日0:00 → **各開催日の前日12:00（正午）**（8/10・8/30・9/3） |
| FV 見出し | 「しあわせを届ける仕事。」→「**幸せ**を届ける仕事。」 |
| FV リード | 「…体験できる採用説明会です。」→「…体験できる**オープン・カンパニー**です。」 |
| FV 参加特典 | 「特別選考ルートをご案内」→「**次回ステップをご案内！**」／下の注記文は削除 |
| SECTION 02 | 「実際に働く社員との座談会」→「社員との座談会」 |
| SECTION 03 | 01と02を入れ替え（**ロマンライフを知る**が先）／「販売職を体験する」→「**販売スタッフ**を体験する」 |
| SECTION 04 | 順番を **受付 → ロマンライフ → 社内見学 → お菓子業界 → 販売スタッフGW → 座談会 → 今後のステップ** に変更／「若手社員・採用担当者との座談会」→「**採用担当者との座談会**」／07の説明文を削除 |
| SECTION 05 | 「一杯を淹れる」→「一杯**のコーヒー**を淹れる」／「幸せ必需品」「絶対積極の精神」の文章を差し替え |
| SECTION 06 | キャリアの順序を **販売・接客 → 副店長・店長・リーダー → 店舗運営・スタッフ育成 → …** に変更／「『販売職＝ずっと店頭に立ち続ける仕事』ではありません。」を削除 |
| SECTION 07 | 「企画から、製造、…」→「**購買**から、製造、…」／フロー01「商品企画」→「**購買**」／結び「ロマンライフのしあわせ」→「ロマンライフの**幸せ**」 |
| SECTION 08 | 「若手社員と採用担当者との座談会」→「採用担当者との座談会」／「数年前まで就職活動をしていた先輩」→「数年前まで**販売職として勤務していた先輩**」 |
| SECTION 09 | 「若手社員に直接質問してみたい」を削除 |
| FAQ | いただいたテキストの内容に差し替え（締切／服装／持ち物／選考／締切前の受付終了） |
| 申込フォーム | **個人情報の取り扱いについて**の全文を掲載（スクロールできる枠内） |
| 住所 | 〒607-8326 → **〒607-8134**（アクセス・フッターの両方） |

### 未反映（素材待ち）

| 項目 | 状況 |
| --- | --- |
| **ブランドロゴの差替（赤字「差替」）** | 公式ロゴデータが必要です。現在はご支給FV画像からの切り出しのままです |
| **写真の差し替え（AI生成 → 実写）** | Google Drive の写真フォルダは、この実行環境のネットワークポリシーで `drive.google.com` への接続が遮断されており取得できませんでした。**チャットに画像を添付いただければ差し替えます** |


---

## 11. ご支給写真の反映（実写）

`photos/` にいただいた写真を、次のとおり配置しました。元ファイルは `photos/` に残しています。

| 元ファイル | 配置先 | 生成した画像 |
| --- | --- | --- |
| `M_vol34_40th2022kita1_rt.jpg`（京都北山本店前の集合写真） | ファーストビュー | `fv-staff.webp`（1800×1059）／`fv-staff-sp.webp`（1200×900） |
| `M_2024_kamikatsura_64_s.jpg`（売場での接客） | SECTION 02 大きい写真 | `intro-handover.webp`（1000×625） |
| `M_2021_JRrm_073_s.jpg`（ドリンクの受け渡し） | SECTION 02 小さい写真 | `intro-family.webp`（760×507） |
| `茶の菓イメージ.jpg` | SECTION 02 小さい写真 | `intro-sweets.webp`（760×507） |
| `ロマンの森_外観.jpg` | SECTION 03「ロマンライフを知る」 | `benefit-02.webp`（900×580） |

### ファーストビューの構成変更

集合写真が横長のため、**「左にコピー＋右に縦長写真」から「コピーの下に横長写真を敷く」構成へ変更**しました。

- スマートフォンは寄りの4:3、PCは16:7（1100px以上は21:8）で表示します
- `<picture>` で画面幅に応じて2種類を出し分けています
- 写真の上端はアイボリーへ溶かして、コピー部分と自然につなげています

### まだAI生成画像のままの箇所

SECTION 04（プログラム7点）、SECTION 05（会社紹介2点）、SECTION 06（キャリア2点）、
SECTION 08（座談会3点）、SECTION 03 の 01以外の3点。
実写をいただければ同じ手順で差し替えます。
